const initialState = {
  word: '',
  etymology: [[]],
  languages: [],
  currentLocation: 
  {
    lat: 0, lng: 0
  }
}

const reducer = (state = initialState, action ) => {
  switch (action.type) {
    default: 
      return state
  }
}

export default reducer